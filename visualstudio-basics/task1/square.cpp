#include <iostream>
#include <cmath>
using namespace std;

const float pi = 3.14;

int main() {
    float s, r, a, squares, squarep;
    cout << "Площа кола S" << endl;
    cin >> s;
    r = sqrt(s / pi);
    a = r * 2;
    kvads = pow(a, 2);
    kvadp = 4 * a;
    cout << "Площа квадрата = " << squares << endl;
    cout << "Периметр квадрата = " << squarep;
}